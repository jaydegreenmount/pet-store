<?php

/*
*Creating drupal hook_theme
*/

function petstorefriends_controller_theme($existing, $type, $theme, $path) {

    return array(
        'artilce_list' => array(
            'variables' => array('items' => array(), 'title' => '')
        )
    );
}